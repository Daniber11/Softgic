apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mfeIndicadores.nombre }}
  labels:
    app.kubernetes.io/name: {{ .Values.mfeIndicadores.nombre }}
    {{- include "solicitudes-gov.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: {{ .Values.mfeIndicadores.nombre }}
  ports:
    - name: http
      port: 80
      targetPort: http
