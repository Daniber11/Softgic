apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.solicitudesService.nombre }}
  labels:
    app.kubernetes.io/name: {{ .Values.solicitudesService.nombre }}
    {{- include "solicitudes-gov.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: {{ .Values.solicitudesService.nombre }}
  ports:
    - name: http
      port: {{ .Values.solicitudesService.puerto }}
      targetPort: http
